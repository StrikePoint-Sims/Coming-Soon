// Temporary placeholder — the full marketing site migration happens in Week 2.
// The existing static index.html remains live via Vercel's legacy static serving
// until Next.js routes replace it.

export default function HomePage() {
  return (
    <main>
      <p style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
        StrikePoint Sims — coming soon.
      </p>
    </main>
  )
}
